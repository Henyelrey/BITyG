name: 'tyg_bi'
version: '1.0.0'
profile: 'tyg_bi'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  tyg_bi:
    staging:
      +materialized: view
    marts:
      +materialized: table
